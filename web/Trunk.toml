[build]
# Assets are served under /app on the server
public_url = "/app/"

# Proxy API requests to the Axum server when using `trunk serve`.
# Has no effect on `trunk build` (used by run_server.sh).
[[proxy]]
backend = "http://localhost:3000/api"
